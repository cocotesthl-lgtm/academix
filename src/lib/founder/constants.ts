/**
 * Constantes compartidas del módulo founder.
 * Vive aparte de actions.ts porque ese archivo es 'use server'
 * (solo puede exportar async functions).
 */
export const IMPERSONATE_COOKIE = 'cp_as_tenant';
